{
  "name": "modbus-manager-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "typecheck": "vue-tsc -b",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modbus-manager/device-catalog-domain": "file:../common/device-catalog-domain",
    "ajv": "^8.20.0",
    "vue": "^3.5.21"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "@vitejs/plugin-vue": "^6.0.1",
    "typescript": "~5.8.3",
    "vite": "^7.1.5",
    "vitest": "^5.0.1",
    "vue-tsc": "^3.0.6"
  }
}
